{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2020",
    "module": "CommonJS",
    "moduleResolution": "node",
    "types": ["node", "vitest"],

    /* Paths and Compilation */
    "rootDir": "./src",      /* 👈 Ensures TypeScript treats `src/` as the root */
    "outDir": "./dist",      /* 👈 Ensures compiled files go to `dist/` */

    /* Interop Constraints */
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,

    /* Type Checking */
    "strict": true,
    "skipLibCheck": true
  },
  "include": [
    "src",          /* 👈 Ensures all TypeScript files in `src/` are compiled */
    "tests"         /* 👈 If you have a `tests/` folder outside of `src/`, include it */
  ],
  "exclude": [
    "node_modules",
    "dist"          /* 👈 Prevents TypeScript from compiling already compiled files */
  ]
}
